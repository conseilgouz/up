; ----- DOCUMENTATION
shortdesc="Creation of a 'sitemap.xml' file in the root website"
longdesc="syntax {up sitemap}"

; explanation of options
sitemap="menutype name to exclude (same option menutype-exclude)"
cron="delay  between 2 automatic generations"
frequency="frequency :  always, hourly, daily, weekly, monthly, yearly, never"
priority="priority (0.1 à 1)"
menutype-exclude="menutype name to exclude  (same as main option)"
info="display the number of links and the list of non-indexed pages"

;messages
VAL_CONFIG_INDEX="Value in global site configuration : %s"
VAL_CAT_INDEX="The category %s (%s) is in no-index"
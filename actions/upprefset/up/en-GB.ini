; ----- DOCUMENTATION
shortdesc="Displays a list of all UP presets for the site."
longdesc="syntax : {up upPrefSet}"

; explanation of options
st-sel="Selection of actions"
upprefset="list of actions or blank for all"
action-exclude="1: all actions except those of the main parameter"
prefset-exclude="options excluded sections"
st-form="Layout format" 
action-template="Section title for action"
prefset-template="model for a prefset"
info-template="template for information"
prefset-separator="separator between presets"
options-separator="underscores are replaced by spaces"
st-exp="file export" 
export-prefs="subfolder of TMP to save the tree structure.. ex : up-pref-foo"

class="class(es) for block"
style="inline style for block"
css-head="CSS rules defined by the webmaster (added in the head)"
action-class="classes and style for the action block"

EXPORT_PREFS_OK="Export to folder <code>%s</code> successful"
COPYFILE_ERR="File copy failure : %s"
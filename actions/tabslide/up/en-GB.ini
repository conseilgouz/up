; ----- DOCUMENTATION
shortdesc="add tabs to open a panel at window sides "
longdesc="syntax {up tabslide=btn-text | tabLocation=top} content {/up tabslide}<br>use :<br>- a summary menu on the site side<br>- a connection unit"

; explanation of options
st-title="the tab"
tabslide="tab title"
tab-style="classes and inline styles for tab"
tab-class="class(es) to tab (obsolete)"
st-panel="the panel"
panel-style="classes and inline styles to panel"
panel-class="class(es) to panel (obsolete)"
st-obsolet="JS options gone - kept for compatibility"
speed="time to animate"
positioning=" fixed, absolute, so tabs move when window scrolls"
toggleButton=" not often used"
st-tab="JS settings: tab definition"
tabLocation="left, right, top or bottom"
onLoadSlideOut="slide out after DOM load"
clickScreenToClose="close tab when rest of screen clicked"
tabHandle="jQuery selector for the tab, can use #"
action="dual action 'hover' or 'click'"
hoverTimeout="ms to keep tab open after no longer hovered - only if action = 'hover'"
offset="distance to left or top (or bottom right if offsetReverse is true)"
offsetReverse="true = aligned right or bottom"
otherOffset="if set, panel size is set to maintain this dist from bottom or right (top left if offsetReverse"
handleOffset="e.g. '10px'. If null, Detects panel border to handle align nicely"
handleOffsetReverse="if true, handle aligned with right or bottom of pane"
st-anim="JS settings: Animation"
bounceDistance="how far bounce event will move everything"
bounceTimes="How Many bounces when 'bounce' is called"
bounceSpeed="time to animate bounces"
st-img="JS settings: image for the tab"
pathToTabImage="optional image to show in the tab"
imageHeight="image height"
imageWidth="image width"
st-expert="JS settings: for expert"
onOpen="handler called after opening"
onClose="handler called after closing"
onSlide="handler called after opening or closing"

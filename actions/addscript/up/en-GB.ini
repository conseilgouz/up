; ----- DOCUMENTATION
shortdesc="adds code or file JS or JQuery"
longdesc="By default, the code is added to the head.<br>If the main parameter is body, the code is inserted at the shortcode position"

; explanation of options
addscript="head or body to insert the code at the shortcode position"
jquery="1 : encapsulated with jquery call (except file)"
filter="conditions. See doc action filter (v1.8)"

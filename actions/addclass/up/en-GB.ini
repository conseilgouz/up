; This file contains the English  translations for this action
; if an info is not translated, we use the doc in the script

; ----- DOCUMENTATION
; summary after action name
shortdesc="adds a class to a CSS selector (default body)"
; documentation before credits
longdesc="It is possible:<br>- to target a parent of the CSS selector<br>- to create the CSS rule with the option 'base-css=.foo[color:red]'.<br>syntax : {up addclass=class_name}<br>Use : change the background image from an article"

; explanation of options
addclass="class name added to the tag"
selector="target beacon. Do not forget the point for a class or the # for an ID"
parent="1 to target selector's parent, 2 for grandfather, ..."
filter="conditions. See doc action filter (v1.8)"
css-head="CSS code for head. Warning, use[] instead of {}"
st-annexe="secondary options"

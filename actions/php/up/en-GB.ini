; ----- DOCUMENTATION
shortdesc="allows you to execute PHP code in an article."
longdesc="Examples :<br>current date: {up php=echo date('d-m-Y H:i:s');}<br>language: {up php=echo JFactory::getLanguage()getTag(); }<br>name user : {up php=$user = Factory::getApplication()->getIdentity();echo  ($user->guest!=1) ? $user->username : 'guest';}"

; explanation of options
php=" the PHP code"
tag="tag used for classes, styles and id if class or style defined"
class="class(es) or style for the return block"
style="class(es) or style for the return block"
st-divers="Miscellaneous"
filter="conditions. See doc action filter (v1.8)"

; ----- DOCUMENTATION
shortdesc="Add a counter with prefix and suffix"
longdesc="Syntax : {up counter=0,100}"

; explanation of options
counter="min,max : start and end values"
width="minimum width (default in pixels)"
mono="force monospace font"

st-annexe="style and secondary options"
class="class for block"
style="inline style for block"
css-head="CSS rules defined by the webmaster (add in the head)"
filter="conditions. See doc action filter (v1.8)"

st-JS="Javascript parameters for counter configuration"
start="start value of the counter"
end="counter reading"
easing="swing or linear : effect"
duration="countdown time in milliseconds"
prefix="text before counter. HTML allowed"
suffix="text after counter. HTML allowed"

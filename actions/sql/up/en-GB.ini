; ----- DOCUMENTATION
shortdesc="SQL query with formatting and sorting"
longdesc="syntax : {up sql=tablename | ...}"

; explanation of options
sql="table name"
select="column lists"
where="where condition"
order="order condition"
group="group condition"
innerjoin="innerjoin condition"
outerjoin="outerjoin condition"
leftjoin="leftjoin condition"
rightjoin="rightjoin condition"
setlimit="setlimit condition"

st-info="database information"
dbinfos="if empty=table list, if table_name=column list"
no-prefix-auto="use the table name without adding the prefix #__"

st-form="formatting of the result"
presentation="presentation of the result : list,table,div or 0"
header="1 or empty to use keywords, otherwise titles separated by semicolons"
overflow="For table presentation : 0 so as not to encapsulate the table in a div for overflow. Useful if use of a responsive action"
sort="column sort type in the form: i,3-f-s. i:int, s:string, f:float. ,3 indicates a secondary sort on the 3rd column"
sort-first="name or position of the column sorted first"
model="name of the model class in the csv2table.css file"
col="alignment and width of the columns in the form x-D-C5-100. for nothing-right-center : 5%-100% (see doc)"
template="layout template"
no-content-html="text if the query returns no result"
st-css="CSS style to display the result"
main-class="up class(es) for block"
main-style="inline style for block"
item-class="class(es) for item"
item-style="inline style for item"
css-head="style added in the HEAD of the page"

UNKNOWN_COLUMN="%s : unknown column in query"
UNKNOWN_TAG="%s : unknown tag in query"
UNKNOWN_TYPE="%s : unknown type"
TITLE_TABLE="List of tables	"
TITLE_COLUMNS="List of columns of %s"
; ----- DOCUMENTATION
shortdesc="responsive tables by stacking the rows of a column."
longdesc="Syntax {up table-by-column}<br>< table> ... < /table><br>{/up table-by-column}<br>The rows are stacked in columns. Very practical for planning purposes"

; explanation of options
table_by_columns="nothing"
id=""
breakpoint="switch to responsive view"
model="name of a CSS file planned by the webmaster for all tables in the
page"
style="inline style for table"
class="class(es) for table tags (obsolete)"
max-height="max-height for the table"
key-width="to block the header at the top"
title-style="style for the title line in responsive view"
css-head="CSS rules put in the head"

UNALLOWED_TAG = "Unauthorized HTML tag(s) : %s"
